@model IEnumerable<kinnemed05.Models.concepto>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.con_resultado)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.con_observacion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.historia.his_motivo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.con_resultado)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.con_observacion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.historia.his_motivo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
