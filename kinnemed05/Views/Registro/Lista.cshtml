
@{
    ViewBag.Title = "Lista";
}
<script>
    $(function () {
        //$("#reg_area").change(function () {
        //    $.ajax({
        //        url: $("link_form").attr("href"),
        //        type: "GET",
        //        data: $("#reg_area").val(),
        //        success: function (data) {
        //            $("#div_form").html(data);
        //        },
        //        error: function (xhr, status, error) {
        //            alert(error);
        //        }

        //    });
        //});
    });
</script>

<div class="col-lg-12">
    <div class="panel panel-default">
        <div class="panel-heading">
            <h3 class="panel-title">Áreas de Exámenes</h3>
        </div>
        <div class="panel-body">
            @using (Ajax.BeginForm("Edit","Resultado", new AjaxOptions { UpdateTargetId = "div_form", HttpMethod="Get" }))
            {
                @Html.AntiForgeryToken()
                @Html.ValidationSummary(true)
                <div class="col-lg-3">
                    <div class="form-group">
                        @Html.Label("Registro", new { @class = "control-label", @for = "inputDefault" })
                        @Html.DropDownList("res_id", (IEnumerable<SelectListItem>)@ViewBag.res_registro, new { @multiple = "", @class = "form-control" })
                    </div>
                </div>

                <div class="col-lg-3">
                    <br />
                    <div class="form-group">
                        <input type="submit" value="Filtrar" class="btn btn-primary" />
                    </div>
                </div> 
            }
        </div>
    </div>
</div>





